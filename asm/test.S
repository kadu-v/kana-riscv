.globl _start

.text
_start:
    li t0, 0x100        // t0 レジスタにメモリアドレス 0x100 をロード
    li t1, 1            // t1 レジスタに値 1 をロード
    sw t1, 0(t0)        // メモリアドレス 0x100 に値 1 をストア
    lw x3, 0(t0)        // メモリアドレス 0x100 から値をロードして x2 レジスタに格納
_loop:
  j _loop